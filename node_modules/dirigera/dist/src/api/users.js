"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.default = (got) => {
    return {
        async list() {
            return (await got.get(`users`).json());
        },
        async getCurrentUser() {
            return (await got.get(`users/me`).json());
        },
        async setCurrentUserName({ name }) {
            await got.patch(`users/me`, {
                json: {
                    name,
                },
            });
        },
        async delete({ id }) {
            await got.delete(`users/${id}`);
        },
    };
};
