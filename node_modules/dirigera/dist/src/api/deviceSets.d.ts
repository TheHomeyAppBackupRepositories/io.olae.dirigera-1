import type { Got } from 'got';
declare const _default: (got: Got) => {
    list(): Promise<import("..").DeviceSet[]>;
    setIsOn({ id, isOn }: {
        id: string;
        isOn: boolean;
    }): Promise<unknown>;
    setAttributes({ id, attributes, transitionTime, }: {
        id: string;
        attributes: Record<string, any>;
        transitionTime?: number;
    }): Promise<unknown>;
};
export default _default;
